---
phase: 02-module-extraction-type-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/url-builder.ts
  - src/lib/url-builder.test.ts
  - src/lib/setup-shortcuts.ts
  - src/lib/setup-shortcuts.test.ts
  - src/lib/record-context.ts
  - src/lib/record-context.test.ts
  - src/lib/navigation.ts
  - src/lib/navigation.test.ts
  - src/lib/typed-event-emitter.ts
  - src/lib/typed-event-emitter.test.ts
  - src/lib/window-manager.ts
  - src/lib/window-manager.test.ts
autonomous: true
requirements: [MODL-01, MODL-03, MODL-04, TEST-02]

must_haves:
  truths:
    - "url-builder.ts exports getSetupHost, buildSetupUrl, resolveSetupShortcutPath, getCurrentRecordFromUrl, shouldUseLightning as standalone pure functions"
    - "setup-shortcuts.ts exports SETUP_SHORTCUTS constant and SetupShortcut type"
    - "record-context.ts exports all record context functions as pure functions taking sfHost parameter"
    - "navigation.ts exports buildNavigationUrl, buildIdNavigationUrl, buildActionUrl and KEY_PREFIX_MAP"
    - "typed-event-emitter.ts exports TypedEventEmitter class with typed on/off/emit"
    - "window-manager.ts re-exports all extracted functions via facade pattern"
    - "All 231 existing tests pass after extraction"
    - "Each extracted module is under 400 lines"
    - "Unit tests exist for every extracted module"
  artifacts:
    - path: "src/lib/url-builder.ts"
      provides: "URL construction pure functions"
    - path: "src/lib/setup-shortcuts.ts"
      provides: "SETUP_SHORTCUTS constant and type"
    - path: "src/lib/record-context.ts"
      provides: "Record context resolution functions"
    - path: "src/lib/navigation.ts"
      provides: "Navigation URL building functions"
    - path: "src/lib/typed-event-emitter.ts"
      provides: "Generic typed event emitter"
    - path: "src/lib/window-manager.ts"
      provides: "Slimmed orchestration + facade re-exports"
  key_links:
    - from: "src/lib/window-manager.ts"
      to: "src/lib/url-builder.ts"
      via: "re-export and direct import"
      pattern: "export.*from.*url-builder"
    - from: "src/lib/navigation.ts"
      to: "src/lib/url-builder.ts"
      via: "import for URL construction"
      pattern: "import.*from.*url-builder"
    - from: "src/lib/window-manager.ts"
      to: "src/lib/typed-event-emitter.ts"
      via: "class extends TypedEventEmitter"
      pattern: "extends TypedEventEmitter"
---

<objective>
Extract window-manager.ts (1716 lines) into 5 focused modules with unit tests for each, plus a typed event emitter (MODL-03). The original window-manager.ts becomes a slimmed-down orchestration class (~600 lines) that re-exports all extracted functions via the facade pattern (D-01), keeping all external importers unchanged.

Purpose: Decompose the largest file in the codebase into independently testable modules following pure-functions-first extraction order (D-02).
Output: 5 new module files + 5 test files + slimmed window-manager.ts + typed-event-emitter.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-module-extraction-type-safety/02-CONTEXT.md
@.planning/phases/02-module-extraction-type-safety/02-RESEARCH.md
@.planning/phases/01-foundation-safety-net/01-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/types/index.ts:
```typescript
export interface SearchResult { ... }  // Used by navigation.ts
export type NavigationMode = 'auto' | 'lightning' | 'classic'
export interface RecordContext { ... }  // Used by record-context.ts
```

From src/lib/auth.ts:
```typescript
export async function sfRest(sfHost: string, path: string, options?: { method?: string; body?: any; api?: 'normal' | 'bulk' }): Promise<any>
export const API_VERSION = '62.0'
export async function getSfHost(url: string): Promise<string | null>
export async function getSession(sfHost: string): Promise<SfSession | null>
```

From src/components/search/ResultItem.tsx:
```typescript
export type ObjectAction = { ... }  // Used by navigation.ts handleActionClick
```

From src/lib/window-manager.ts (current state, lines 12-25):
```typescript
interface WindowManagerState {
  isVisible: boolean; isInitialized: boolean; sfHost: string | null
  searchResults: Record<string, SearchResult[]>; isLoading: boolean
  hasSession: boolean; closeOnNavigate: boolean; navigationMode: NavigationMode
  fuzzySearch: boolean; searchError: string | null
  userLightningPreference: boolean | null; recordContext: RecordContext | null
}
```

From src/lib/window-manager.ts (lines 38-67):
```typescript
type SetupShortcut = { id: string; name: string; description: string; path: string; classicPath?: string }
const KEY_PREFIX_MAP: Record<string, string> = { '001': 'Account', '003': 'Contact', ... }
const SETUP_SHORTCUTS: SetupShortcut[] = [...]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract pure modules (url-builder, setup-shortcuts, typed-event-emitter) with tests</name>
  <files>
    src/lib/url-builder.ts, src/lib/url-builder.test.ts,
    src/lib/setup-shortcuts.ts, src/lib/setup-shortcuts.test.ts,
    src/lib/typed-event-emitter.ts, src/lib/typed-event-emitter.test.ts,
    src/lib/window-manager.ts, src/lib/window-manager.test.ts
  </files>
  <read_first>
    src/lib/window-manager.ts (full file - source of extraction),
    src/lib/window-manager.test.ts (characterization tests - must stay green),
    src/types/index.ts (shared types),
    .planning/phases/02-module-extraction-type-safety/02-RESEARCH.md (extraction patterns and line references)
  </read_first>
  <behavior>
    url-builder.test.ts:
    - getSetupHost('myorg.my.salesforce.com') returns 'myorg.my.salesforce-setup.com'
    - getSetupHost('myorg.lightning.force.com') returns setup equivalent
    - getSetupHost(null) returns null
    - buildSetupUrl(host, '/path') returns full https URL
    - buildSetupUrl(null, '/path') returns null
    - shouldUseLightning('lightning', null) returns true
    - shouldUseLightning('classic', null) returns false
    - shouldUseLightning('auto', true) returns true
    - shouldUseLightning('auto', false) returns false
    - getCurrentRecordFromUrl extracts record ID from Lightning URL
    - resolveSetupShortcutPath resolves paths correctly

    setup-shortcuts.test.ts:
    - SETUP_SHORTCUTS is an array with length > 0
    - Each shortcut has id, name, description, path fields
    - SetupShortcut type is exported

    typed-event-emitter.test.ts:
    - on() registers handler, emit() calls it with typed data
    - off() removes handler, emit() no longer calls it
    - Multiple handlers on same event all fire
    - Handler error does not prevent other handlers from firing
    - clearAll() removes all handlers
  </behavior>
  <action>
    **Step 1: Create url-builder.ts** (per D-02, D-04)
    Extract these functions from window-manager.ts (lines 149-233) into src/lib/url-builder.ts:
    - `resolveSetupShortcutPath` (line 149) - pure function, no this reference
    - `getSetupHost` (line 157) - pure function, chain of .replace() calls
    - `buildSetupUrl` (line 170) - calls getSetupHost, returns URL string
    - `getCurrentRecordFromUrl` (line 176) - reads window.location, returns string|null
    - `shouldUseLightning` (line 192) - pure function taking (navigationMode, userPref)

    All functions use named exports. No default export. No class.

    **Step 2: Create setup-shortcuts.ts** (per D-04)
    Extract from window-manager.ts (lines 38-147):
    - `SetupShortcut` interface (exported as type)
    - `SETUP_SHORTCUTS` constant array (exported)

    **Step 3: Create typed-event-emitter.ts** (per D-11, MODL-03)
    Create src/lib/typed-event-emitter.ts (~50 lines):
    - Define `WindowManagerEvents` interface with event names as keys and payload types as values:
      - `stateChange: { oldState: WindowManagerState; newState: WindowManagerState; updates: Partial<WindowManagerState> }`
      - `resultClick: SearchResult`
      - `actionClick: { result: SearchResult; action: ObjectAction }`
      - `visibilityChange: boolean`
    - Export `TypedEventEmitter<EventMap>` class with:
      - `on<K extends keyof EventMap>(event: K, handler: (data: EventMap[K]) => void): void`
      - `off<K extends keyof EventMap>(event: K, handler: (data: EventMap[K]) => void): void`
      - `protected emit<K extends keyof EventMap>(event: K, data: EventMap[K]): void`
      - `clearAll(): void`
    - Use `Map<keyof EventMap, Set<(data: any) => void>>` internally (the `any` is internal only, public API is typed)
    - Wrap handler calls in try/catch, log errors via `logger.error`

    **Step 4: Write tests** (RED then GREEN)
    Write url-builder.test.ts, setup-shortcuts.test.ts, typed-event-emitter.test.ts with the behaviors listed above. Use `vi.stubGlobal` for window.location in url-builder tests. Use `describe/it` with "should" descriptions per project convention.

    **Step 5: Update window-manager.ts facade**
    - Add re-exports at top of window-manager.ts:
      ```
      export { getSetupHost, buildSetupUrl, resolveSetupShortcutPath, getCurrentRecordFromUrl, shouldUseLightning } from './url-builder'
      export { SETUP_SHORTCUTS } from './setup-shortcuts'
      export type { SetupShortcut } from './setup-shortcuts'
      ```
    - Remove the extracted function bodies from window-manager.ts (but import them for internal use)
    - Replace `Set<Function>` event handlers: make UltraForceWindowManager extend `TypedEventEmitter<WindowManagerEvents>` or use composition (use `this.emit(...)` with typed events)
    - Remove the old `on()`, `off()`, `emit()`, `clearAll()` methods from the class (inherited from TypedEventEmitter)
    - Run `rtk vitest run --run` to confirm all 231 tests still pass
  </action>
  <verify>
    <automated>cd /Users/dormonzhou/Projects/ultraforce && rtk vitest run --run</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/url-builder.ts exists and contains `export function getSetupHost`
    - src/lib/url-builder.ts contains `export function buildSetupUrl`
    - src/lib/url-builder.ts contains `export function shouldUseLightning`
    - src/lib/url-builder.ts contains `export function getCurrentRecordFromUrl`
    - src/lib/url-builder.ts contains `export function resolveSetupShortcutPath`
    - src/lib/setup-shortcuts.ts exists and contains `export const SETUP_SHORTCUTS`
    - src/lib/setup-shortcuts.ts contains `export type SetupShortcut` or `export interface SetupShortcut`
    - src/lib/typed-event-emitter.ts exists and contains `export class TypedEventEmitter`
    - src/lib/typed-event-emitter.ts contains `on<K extends keyof EventMap>`
    - src/lib/typed-event-emitter.ts contains `off<K extends keyof EventMap>`
    - src/lib/typed-event-emitter.ts contains `protected emit<K extends keyof EventMap>`
    - src/lib/window-manager.ts contains `export { getSetupHost` (facade re-export)
    - src/lib/window-manager.ts contains `export { SETUP_SHORTCUTS` (facade re-export)
    - src/lib/window-manager.ts does NOT contain `Set<Function>` (replaced by TypedEventEmitter)
    - url-builder.test.ts exits 0
    - setup-shortcuts.test.ts exits 0
    - typed-event-emitter.test.ts exits 0
    - All 231+ existing tests pass (vitest run exits 0)
    - `wc -l src/lib/url-builder.ts` is under 200 lines
    - `wc -l src/lib/setup-shortcuts.ts` is under 200 lines
    - `wc -l src/lib/typed-event-emitter.ts` is under 100 lines
  </acceptance_criteria>
  <done>url-builder.ts, setup-shortcuts.ts, typed-event-emitter.ts extracted with tests. window-manager.ts has facade re-exports and uses TypedEventEmitter. All existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract stateful modules (record-context, navigation) with tests, slim down window-manager.ts</name>
  <files>
    src/lib/record-context.ts, src/lib/record-context.test.ts,
    src/lib/navigation.ts, src/lib/navigation.test.ts,
    src/lib/window-manager.ts, src/lib/window-manager.test.ts
  </files>
  <read_first>
    src/lib/window-manager.ts (current state after Task 1),
    src/lib/window-manager.test.ts (characterization tests),
    src/lib/url-builder.ts (dependency for navigation),
    src/lib/auth.ts (sfRest dependency for record-context),
    src/types/index.ts (SearchResult, NavigationMode, RecordContext types),
    src/components/search/ResultItem.tsx (ObjectAction type for navigation)
  </read_first>
  <behavior>
    record-context.test.ts:
    - fetchRecordTypeId(sfHost, objectApiName, recordId) returns record type ID string
    - fetchRecordTypeId returns null when sfRest throws
    - getCurrentUserId(sfHost) returns user ID string from Tooling API
    - getCurrentUserProfileId(sfHost, userId) returns profile ID string
    - getUserLightningPreference(sfHost, userId) returns boolean
    - getLayoutAssignment(sfHost, objectApiName, recordTypeId, profileId) returns layout URL
    - resolveObjectApiNameFromRecord(sfHost, recordId) uses sobject describe endpoint
    - Module-level caches (sobjectPrefixCache, etc.) prevent duplicate API calls

    navigation.test.ts:
    - buildNavigationUrl for SetupShortcut result returns result.url directly
    - buildNavigationUrl returns null when sfHost is null
    - buildNavigationUrl for ApexClass in Lightning mode returns /lightning/setup/ApexClasses/page?...
    - buildNavigationUrl for ApexClass in Classic mode returns /setup/... classic URL
    - buildIdNavigationUrl for 15-char ID resolves via KEY_PREFIX_MAP
    - buildActionUrl for Edit action returns correct URL
    - KEY_PREFIX_MAP maps '001' to 'Account', '003' to 'Contact', etc.
  </behavior>
  <action>
    **Step 1: Create record-context.ts** (per D-04)
    Extract from window-manager.ts (lines 778-1165) into src/lib/record-context.ts:
    - `fetchRecordTypeId(sfHost: string, objectApiName: string, recordId: string): Promise<string | null>` — calls sfRest to get RecordTypeId
    - `handleFieldsNavigation(sfHost: string, objectApiName: string): Promise<string | null>` — returns setup URL for object fields
    - `handleRecordTypeNavigation(sfHost: string, objectApiName: string): Promise<string | null>` — returns setup URL for record types
    - `handlePageLayoutNavigation(sfHost: string, objectApiName: string, recordId: string | null, recordTypeId: string | null): Promise<string | null>` — returns page layout URL
    - `getCurrentRecordLayoutUrl(sfHost: string, objectApiName: string, recordId: string): Promise<string | null>`
    - `getCurrentRecordLayoutInfo(sfHost: string): Promise<RecordContext | null>`
    - `resolveObjectApiNameFromRecord(sfHost: string, recordId: string): Promise<string | null>` — uses sfRest describe endpoint + module-level sobjectPrefixCache (Map)
    - `getCurrentUserId(sfHost: string): Promise<string | null>` — executes anonymous Apex via Tooling API
    - `getCurrentUserProfileId(sfHost: string, userId: string): Promise<string | null>`
    - `getUserLightningPreference(sfHost: string, userId: string): Promise<boolean | null>`
    - `getLayoutAssignment(sfHost: string, objectApiName: string, recordTypeId: string | null, profileId: string | null): Promise<string | null>`

    Key change: All methods that previously accessed `this.state.sfHost` now take `sfHost` as a parameter. Module-level Maps replace instance caches (`sobjectPrefixCache`, `currentUserProfileId` cache). Import `sfRest` and `API_VERSION` from `./auth`, import `getSetupHost` from `./url-builder`.

    **Step 2: Create navigation.ts** (per D-04)
    Extract from window-manager.ts:
    - `KEY_PREFIX_MAP` constant (lines 47-67) — exported Record<string, string>
    - `buildNavigationUrl(result: SearchResult, context: NavigationContext): string | null` — extracted from handleResultClick (lines 1183-1492), refactored from 300-line switch to pure function. Takes NavigationContext = { sfHost: string; navigationMode: NavigationMode; userLightningPreference: boolean | null }.
    - `buildIdNavigationUrl(id: string, context: NavigationContext): string | null` — extracted from handleIdNavigate (lines 1494-1503)
    - `buildActionUrl(result: SearchResult, action: ObjectAction, context: NavigationContext): string | null` — extracted from handleActionClick (lines 1505-1597)

    Define and export `NavigationContext` interface. Import `getSetupHost`, `shouldUseLightning` from `./url-builder`. DRY up the repeated `getSetupHost` calls in the Lightning branch by computing `setupHost` once at the top.

    **Step 3: Write tests** (RED then GREEN)
    - record-context.test.ts: Mock `sfRest` from `./auth` using `vi.mock('./auth')`. Test each function with mock responses. Verify caches prevent duplicate calls (call twice, sfRest called once).
    - navigation.test.ts: No mocks needed (pure functions). Use `it.each` with test cases derived from characterization tests. Test Lightning and Classic mode for each metadata type.

    **Step 4: Update window-manager.ts facade + slim down**
    - Add re-exports:
      ```
      export { buildNavigationUrl, buildIdNavigationUrl, buildActionUrl, KEY_PREFIX_MAP } from './navigation'
      export type { NavigationContext } from './navigation'
      export { fetchRecordTypeId, getCurrentRecordLayoutInfo, resolveObjectApiNameFromRecord, getCurrentUserId, getCurrentUserProfileId, getUserLightningPreference, getLayoutAssignment } from './record-context'
      ```
    - Remove extracted function bodies from class. Class methods now delegate:
      ```
      async handleResultClick(result: SearchResult) {
        const url = buildNavigationUrl(result, {
          sfHost: this.state.sfHost!,
          navigationMode: this.state.navigationMode,
          userLightningPreference: this.state.userLightningPreference
        })
        if (url) { chrome.tabs.create({ url }) }
      }
      ```
    - Verify window-manager.ts is ~600 lines or less (under 800-line project max)
    - Run `rtk vitest run --run` to confirm all tests pass
    - Run `rtk tsc --noEmit` to confirm no type errors
  </action>
  <verify>
    <automated>cd /Users/dormonzhou/Projects/ultraforce && rtk vitest run --run && rtk tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/record-context.ts exists and contains `export async function fetchRecordTypeId`
    - src/lib/record-context.ts contains `export async function getCurrentUserId`
    - src/lib/record-context.ts contains `export async function getCurrentUserProfileId`
    - src/lib/record-context.ts contains `export async function getUserLightningPreference`
    - src/lib/record-context.ts contains `export async function getLayoutAssignment`
    - src/lib/record-context.ts contains `export async function resolveObjectApiNameFromRecord`
    - src/lib/record-context.ts does NOT contain `this.state` (pure functions, no class)
    - src/lib/navigation.ts exists and contains `export function buildNavigationUrl`
    - src/lib/navigation.ts contains `export function buildIdNavigationUrl`
    - src/lib/navigation.ts contains `export function buildActionUrl`
    - src/lib/navigation.ts contains `export const KEY_PREFIX_MAP`
    - src/lib/navigation.ts contains `export interface NavigationContext`
    - src/lib/window-manager.ts contains `export { buildNavigationUrl` (facade re-export)
    - src/lib/window-manager.ts contains `export { fetchRecordTypeId` (facade re-export)
    - `wc -l src/lib/window-manager.ts` is under 800 lines
    - `wc -l src/lib/record-context.ts` is under 400 lines
    - `wc -l src/lib/navigation.ts` is under 400 lines
    - record-context.test.ts exits 0
    - navigation.test.ts exits 0
    - All existing tests pass (vitest run exits 0)
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>record-context.ts and navigation.ts extracted with tests. window-manager.ts is slimmed to ~600 lines with all facade re-exports. All existing 231+ tests pass. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- `rtk vitest run --run` exits 0 (all tests pass including new + characterization)
- `rtk tsc --noEmit` exits 0 (no TypeScript errors)
- `wc -l src/lib/url-builder.ts src/lib/setup-shortcuts.ts src/lib/record-context.ts src/lib/navigation.ts src/lib/typed-event-emitter.ts src/lib/window-manager.ts` — each file under its target line count
- `grep -c 'Set<Function>' src/lib/window-manager.ts` returns 0
- `grep -c 'export.*from.*url-builder\|export.*from.*setup-shortcuts\|export.*from.*navigation\|export.*from.*record-context' src/lib/window-manager.ts` returns 4+ (facade re-exports present)
</verification>

<success_criteria>
- window-manager.ts decomposed into 5 modules + typed event emitter (MODL-01, MODL-03)
- All extracted modules have unit tests (TEST-02)
- All 231+ existing tests pass (MODL-04)
- Each module under 400 lines, window-manager.ts under 800 lines
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-module-extraction-type-safety/02-01-SUMMARY.md`
</output>
